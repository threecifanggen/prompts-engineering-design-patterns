# List Agent 命令

列出当前项目中所有可用的 subagents（子代理），包括它们的名称、描述和配置信息。

## 执行步骤

1. **扫描 subagent 目录**
   - 检查 `.cursor/agents/` 目录
   - 检查 `.claude/agents/` 目录（如果存在）
   - 检查 `.codex/agents/` 目录（如果存在）

2. **读取 agent 信息**
   - 读取每个 `.md` 文件
   - 解析 YAML frontmatter 获取：
     - `name`: Agent 名称
     - `description`: 功能描述
     - `model`: 使用的模型
     - `readonly`: 是否只读
     - `is_background`: 是否后台运行

3. **生成报告**
   - 按目录分组显示
   - 显示每个 agent 的详细信息
   - 标注配置特性（只读、后台、模型类型等）

## 输出格式

```markdown
# 📊 Project Subagents

## .cursor/agents/ (项目级)

### 🤖 dev
**描述**: Python 开发专家，使用 Python 3.12 和 conda 环境进行开发和测试
**模型**: inherit
**特性**: 
- 需求整理和分析
- Python 3.12 开发
- 完整类型注释
- 自动交给 @formatter

**调用方式**: 
- `/dev` - 显式调用
- `@dev` - 在对话中提及
- 自动调用 - 当需要开发 Python 功能时

---

### ⚡ formatter
**描述**: Python 代码格式化和质量检查专家
**模型**: fast
**特性**:
- Pylint 检查 (≥8.0/10)
- Mypy 类型验证
- Ruff 代码格式化

**调用方式**:
- `/formatter` - 显式调用
- 自动调用 - @dev 开发完成后

---

### 🎯 manager
**描述**: Cursor 上下文管理专家
**模型**: inherit
**特性**:
- 配置优化
- 问题诊断
- 工作流设计

**调用方式**:
- `/manager` - 显式调用
- `@manager` - 管理 Cursor 配置时

---

## 统计信息

- **总计**: 3 个 subagents
- **项目级**: 3 个
- **用户级**: 0 个
- **使用 fast 模型**: 1 个
- **只读模式**: 0 个
- **后台运行**: 0 个

## 使用建议

1. **开发流程**: 使用 `@dev` 进行开发，完成后自动调用 `@formatter`
2. **配置管理**: 使用 `@manager` 优化和管理 Cursor 配置
3. **并行工作**: 可以同时调用多个 subagent 并行处理任务
```

## 详细说明

### Agent 优先级
当多个位置存在同名 agent 时，优先级为：
1. `.cursor/agents/` (最高)
2. `.claude/agents/`
3. `.codex/agents/`

### Agent 状态指示器
- 🤖 标准 agent
- ⚡ 快速模型 (fast)
- 🔒 只读模式 (readonly)
- 🔄 后台运行 (is_background)
- 🎯 专家模式 (特定领域)

### 扩展信息
如果需要查看更详细的信息，可以：
- 阅读 agent 文件：`.cursor/agents/<agent-name>.md`
- 查看 frontmatter 了解完整配置
- 检查 agent 的具体指令和工作流程

## 故障排查

### 没有找到任何 agent
- 检查 `.cursor/agents/` 目录是否存在
- 确认目录中有 `.md` 文件
- 验证文件格式是否正确

### Agent 信息显示不完整
- 检查 frontmatter 格式是否正确
- 确保 YAML 语法无误
- 验证 `name` 和 `description` 字段存在

### Agent 无法调用
- 确认 agent 文件名与 frontmatter 中的 `name` 一致
- 检查文件权限
- 重启 Cursor 重新加载配置

## 相关命令

- `/list-rules` - 列出所有规则
- `/list-commands` - 列出所有命令
- `/list-skills` - 列出所有技能

## 维护建议

1. **定期审查**: 每月检查一次 agent 配置
2. **清理未使用**: 删除不再需要的 agent
3. **更新文档**: 保持 description 和实际功能同步
4. **版本控制**: 将 agent 配置提交到 git
5. **团队共享**: 分享有用的 agent 给团队成员
